<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Thalo – Shipment Tracker</title>
  <link rel="stylesheet" href="/style.css">
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
</head>
<body>
  <header>
    <h1>Thalo – Shipment Tracker</h1>
  </header>

  <main>
    <section class="controls">
      <input type="text" id="searchInput" placeholder="Search by Load ID, Origin, Destination..." />
      <select id="statusFilter">
        <option value="">Filter by Status</option>
        <option value="Draft">Draft</option>
        <option value="Covered">Covered</option>
        <option value="Dispatched">Dispatched</option>
        <option value="At pick up">At pick up</option>
        <option value="Picked up">Picked up</option>
        <option value="En route">En route</option>
        <option value="At receiver">At receiver</option>
        <option value="Delivered">Delivered</option>
        <option value="Ready to bill">Ready to bill</option>
      </select>
    </section>

    <section class="form-section">
      <h2>Add New Load</h2>
      <form id="addForm">
        <input type="text" id="origin" placeholder="Origin" required />
        <input type="text" id="destination" placeholder="Destination" required />
        <select id="status" required>
          <option value="">Select Status</option>
          <option value="Draft">Draft</option>
          <option value="Covered">Covered</option>
          <option value="Dispatched">Dispatched</option>
          <option value="At pick up">At pick up</option>
          <option value="Picked up">Picked up</option>
          <option value="En route">En route</option>
          <option value="At receiver">At receiver</option>
          <option value="Delivered">Delivered</option>
          <option value="Ready to bill">Ready to bill</option>
        </select>
        <button type="submit">Add Load</button>
      </form>
    </section>

    <section class="table-section">
      <h2>All Shipments</h2>
      <table id="shipmentsTable">
        <thead>
          <tr>
            <th>Load ID</th>
            <th>Origin</th>
            <th>Destination</th>
            <th>Status</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody id="shipmentsBody">
          <% shipments.forEach(shipment => { %>
            <tr data-id="<%= shipment.id %>">
              <td><%= shipment.load_id %></td>
              <td><%= shipment.origin %></td>
              <td><%= shipment.destination %></td>
              <td><%= shipment.status %></td>
              <td>
                <button class="editBtn">Edit</button>
                <button class="deleteBtn">Delete</button>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </section>
  </main>

  <footer>
    <p>© <%= new Date().getFullYear() %> Thalo. All rights reserved.</p>
  </footer>

  <script src="/script.js"></script>
</body>
</html>
